#include <soulja_client_lib/config.h>
#include <stdio.h>
#include <string.h>
#include <soulja_client_lib/config.h>

void load_config(int argc, char* argv[]) {
    printf("ARG: %s\n", argv[1]);
    if (strcmp(*(argv+1), "--assets_folder") == 0) {
    }
}
